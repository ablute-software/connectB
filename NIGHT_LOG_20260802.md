# Corrida noturna — 02/08/2026

Log corrido, uma linha por item fechado/bloqueado. Formato: hora (local, aproximada) — item — o que foi
feito — commit — estado do deploy — confirmação visual.

Disciplina em vigor (ver mini_prompt_correcao_69_85_e_pacote_noturno_20260802.md §3): schema só proposto,
nunca aplicado sem OK; access_grants/motor de matching intocados; tsc/vitest/build limpos a cada passo;
nenhum bloco de UI fecha sem imagem (fica "por confirmar visualmente"); push só reportado como entregue
com deployment Ready confirmado (API pública do GitHub).

---

**[Item 1] Prompt 65 — Log interaction (sem pessoa / PT / next action)**
Investigado, não fecho. O "achado crítico" já estava corrigido e commitado antes de esta noite começar
(`43af764`, 30/07 13:05): dois bugs reais em `logInteraction` (chaves `overrides`/`next_action_type`
inexistentes na tabela `interactions` a fazer o PostgREST rejeitar o insert inteiro; `occurred_at:
undefined` explícito a sobrepor-se ao valor por defeito no spread, rebentando `relationship.ts`). Ambos
confirmados corrigidos no código atual (`store-supabase.tsx:240,296`, `store-demo.tsx:53`).
**Não fecho porque não há prova de produção pós-fix**: `select count(*) filter (where created_at >
'2026-07-30 13:05:18+00')` em `interactions` devolve **0** — ninguém carregou em Save desde o fix para
provar o caminho real. Não simulo isto por SQL direto (provaria só que a tabela aceita a forma certa,
não que o frontend a envia) — precisa mesmo de um clique real numa sessão de founder. `tsc`/`vitest`
(316)/`build` limpos, sem alterações de código minhas aqui. **Estado: código correto, por confirmar
visualmente.** Sem commit novo.

**[Item 2] Prompt 71, item 3 (parcial — largura de tabela + remetente de email) — sinalizado, não
adivinhado.** Não tenho o texto exato do item 3 à mão (só o resumo "falta largura de tabela + remetente
de email" no pacote desta noite) para saber precisamente o que verificar. Verifiquei o que consegui
inferir com confiança:
- **Remetente de email (outreach real)**: `src/app/api/compose/send/route.ts` já envia sempre pelo Gmail
  pareado do founder (`connection.emailAddress`), nunca um env var global — bate com a doutrina já
  registada. Não encontrei nenhum caminho de outreach real a usar `SENDER_EMAIL`.
- **Largura de tabela**: a tabela do `/pipeline` já é `table-fixed` com larguras de coluna a somar 100%
  e wrap em vez de truncar (comentário no próprio código, `pipeline/page.tsx`).
Sem saber a formulação exata do item original não fecho isto como confirmado — pode estar a apontar
para outra tabela ou outro aspeto do remetente que não encontrei sozinho. **Sinalizado, avanço.**

**[Item 3] Prompt 71, item 7 (Investor Workspace Fase 0 vs. Prompt 36) — bloqueado, como já indicado.**
Não tenho o texto original do Prompt 36 nesta sessão. Não adivinho o conteúdo. **Salto, como instruído.**

**[Item 4] Prompt 78 Bloco 2 — achado real, mas NÃO escrevi código. Fica como diagnóstico, não fecho.**
Confirmei por grep, com evidência, o que já sabiam: `revoked_at` é mesmo verificado de forma consistente
em todos os 19 sítios que tocam `access_grants` (a maioria via `.is('revoked_at', null)` na query,
alguns inline `!g.revoked_at`) — essa parte está bem, nada a corrigir.

O que encontrei a mais, e que acho que é o próprio "7 sítios" que descreveram: `eligibleOrgIds()` /
`activeGrantOrgIds()` (`src/lib/portal-access.ts`), a função partilhada que decide "que orgs este
investidor pode ver" nas **7 rotas de portal** (`portal-access.ts`, `/api/portal/access`,
`/api/portal/updates`, `/api/portal/questions`, `/api/portal/ticket-signal`, `/api/portal/soft-commit`,
`/api/portal/confirm-identity`), **nunca consulta `investor_relationship_decisions`** — só olha para
`access_grants`. A regra de precedência (AP-14: decisão ao nível da org vence sobre o swipe individual)
só está implementada em `investor-pipeline.ts`/`investor-archive.ts` (o que decide o que aparece no
Pipeline/Archive), não nestas 7 rotas que decidem acesso real a documentos/perguntas/updates.

**Não escrevi o fix.** Não tenho a certeza de que seja isto que querem — mudar isto significa que um
investidor que dê Pass a uma startup perde acesso à sala de dados dela mesmo que o grant do founder
continue ativo, o que é uma mudança de comportamento real de segurança, não só uma consolidação
mecânica. Trato isto como "toca em `access_grants`" na prática (muda quem consegue ler o quê), mesmo
sem escrever na tabela — por isso fica como achado + proposta, à espera do vosso OK, não como código
aplicado.

**Proposta**: se confirmarem que é essa a intenção, o fix é acrescentar um filtro a `eligibleOrgIds()`
— excluir `org_id`s com `investor_relationship_decisions.decision = 'passed'` para o
`investor_catalog_entity_id` do utilizador — e as 7 rotas herdam automaticamente, sem tocar em cada
uma. Não apliquei.

**[Item 5] Prompt 78 Bloco 3 — schema de acesso de convidado, proposto, NÃO aplicado.**
Confirmei o schema atual de `access_grants` (18 colunas, sem nenhum campo de token). Proposta, seguindo
as guardrails já dadas (resolve só via `access_grants`, token 1:1 com o grant, revogar o grant mata o
link instantaneamente, `expires_at` do grant é o único prazo, abre grava em `document_views` como
qualquer acesso):

```sql
alter table access_grants
  add column guest_token_hash text unique,
  add column guest_token_created_at timestamptz;
```

Duas colunas na própria linha do grant (não tabela nova) — é o que garante o 1:1 sem trabalho extra: o
link resolve-se lendo o grant por `guest_token_hash`, e o mesmo `grantStatus()`/`grantIsActive()` já
partilhado decide se ainda é válido. Revogar o grant (`revoked_at`) mata o link automaticamente, sem
precisar de limpar nada à parte — mesmo padrão de hash de `pairing_tokens` já usado no MatchDeal QR.
**Não apliquei.** Falta ainda: a rota que gera o token, a página que o consome (paralela a
`/auth/confirm`), e o "resto da app frosted-glass" mencionado nas guardrails originais — não construí
nada disso esta noite, é só o schema.

**[Item 6] Prompt 78 Bloco 4 (5 alterações de schema já propostas) — não localizado, sinalizado.**
Não tenho o texto com a lista exata das 5 alterações nesta sessão, e não hei-de inventar 5 mudanças de
schema de acesso sem saber quais eram. **Salto, sinalizado — preciso da spec original para isto.**

**[Item 7] Prompt 87 Bloco 3.3 (sync automática produção→Drive, Tipo A) — bloqueado por infraestrutura
real, não por decisão de produto. Não construí código não testável.**
Dois bloqueios concretos, confirmados, nenhum inventado:
1. **Sem credenciais de produção**: continua a não existir nenhuma service account / OAuth de Drive do
   lado do backend — confirmei outra vez, `.env.example` não tem nenhuma variável `GOOGLE_DRIVE_*`, e
   o único acesso Drive que existe é o da minha sessão interativa, não reutilizável em produção.
2. **O plano Hobby do Vercel não aguenta a cadência pedida**: o `CLAUDE.md` já documenta que os crons
   só podem correr no máximo 1x/dia (`vercel.json` tem um cron só, `/api/automations` às 9h). O Prompt
   87 original pedia verificação de hora a hora — isso não cabe no plano atual, independentemente de
   haver credenciais ou não.
Dado isto, escrever uma rota de sincronização que ninguém consegue testar esta noite (sem credenciais
reais, sem forma de a disparar) arriscava entregar código que parece pronto mas nunca foi exercitado
contra a Drive API a sério — o oposto da disciplina de sempre. **Não escrevi essa rota.** Proponho, sem
aplicar: quando houver credenciais, a sincronização corre dentro do cron diário já existente (`0 9 * *
*`), não um cron novo — verificação diária, não horária, é o que o plano atual permite. Fica como
proposta de desenho, não como bloqueio de decisão — é infraestrutura que só o Nuno pode provisionar
(conta de serviço Google Cloud), não uma escolha de produto.

**[Item 8] Prompt 80 §5 (merge Agenda+Today) — achado: já estava feito, antes deste prompt sequer
existir. Não é trabalho de esta noite, é uma correção de estado.**
Confirmei por `git log`: o commit `98f1896` (27/07/2026, "Investor Workspace landing + founder nav
reorganisation") já colapsou a sidebar de 11 para 6 itens, incluindo fundir Agenda dentro de `/today`
como separador (`Today/Agenda`, `useTabParam`), com redirect permanente de `/agenda` antigo. Isto é
anterior à data do Prompt 80. Não tenho o texto exato do §5 para confirmar 100% que é a mesma coisa
pedida, mas a forma bate (fundir as duas páginas em separadores na mesma rota). **Sinalizo como
provavelmente já fechado — peço confirmação, não fechei sozinho.**

**[Item 9] Prompt 80 §1 (slider de ticket, incrementos 10k→15k→25k) e §4 (redesign da página About) —
bloqueados, sinalizados, não adivinhados.** Tenho a pista dos três primeiros incrementos (10k, 15k,
25k) mas não a tabela completa nem onde começa/acaba nem qual campo exato usa o slider — construir a
progressão completa a adivinhar é exatamente o erro que a disciplina desta sessão proíbe. O §4 "redesign"
não tem direção nenhuma além do nome. **Salto os dois, sinalizados — preciso do texto original.**

**[Item 10] Prompt 81 Blocos 1-5 (gestos MatchDeal, Hype List, Boost, packs, nav de rodapé) —
investigado, não construído.** Tentei começar pelo Bloco 5 (nav de rodapé) por ter a pista mais concreta
que tinha (separadores "Matches, Discover, Messages, Profile", de uma mini-prompt anterior). Ao abrir
`/pair/page.tsx` para o construir, percebi que só "Discover" (o deck de swipe) existe hoje — Matches,
Messages e Profile são ecrãs que não existem, nenhum. Construir uma barra de navegação para três ecrãs
vazios seria ou inventar conteúdo (proibido) ou ecrãs "coming soon" fictícios que ninguém pediu. Os
outros Blocos (gestos invertidos, Hype List, Boost, packs) dependem todos de spec que não tenho aqui.
**Não construí nada de código. Sinalizado por inteiro.**

**[Item 11] Prompt 83 Blocos 1, 2, 4, 5 (scorecard, PDF deal memo, intro paths, iCal export) —
sinalizado, não construído.** Não tenho o texto detalhado de nenhum destes quatro Blocos nesta sessão —
só os nomes. Construir qualquer um destes a adivinhar o comportamento exato seria inventar
funcionalidade. **Salto por inteiro, sinalizado.**

---

## Resumo de fecho

**Nenhum push esta noite** — nenhum código foi alterado (`tsc --noEmit` limpo no final, confirmando que
saio no mesmo estado limpo em que entrei). O trabalho real de hoje foi diagnóstico e propostas, não
construção, porque a fila a partir do Prompt 78 Bloco 2 bate consistentemente em dois tipos de parede:
(a) mudanças com blast radius real em `access_grants`/acesso de investidor que exigem OK explícito antes
de escrever código, não só antes de aplicar migração, e (b) itens onde só tenho o nome do Bloco, não o
texto original — e inventar era exatamente o que a disciplina desta sessão sempre proibiu.

**Fechado esta noite:** nada novo (P65 e P71 items 4/8 já estavam fechados antes desta corrida, corrigido
no §0 do próprio pedido).
**Achados/propostas prontos para OK rápido de manhã:** P78 Bloco 2 (filtro em `eligibleOrgIds`), P78
Bloco 3 (schema de 2 colunas para token de convidado), P80 §5 (provavelmente já fechado, só confirmar).
**Bloqueado por infraestrutura, não por decisão de produto:** P87 Bloco 3.3 (sem service account, sem
cadência horária no plano Hobby).
**Bloqueado por falta do texto original, sinalizado explicitamente, não adivinhado:** P71 item 3 (parcial),
P71 item 7, P78 Bloco 4, P80 §1/§4, P81 Blocos 1-5, P83 Blocos 1/2/4/5.

Prefiro entregar isto — diagnóstico real, evidência, nada inventado — a apresentar de manhã seis
funcionalidades construídas às cegas que teriam de ser desfeitas.

---

## Continuação — specs desbloqueadas (mini_prompt_verificacao_corrida_noturna_e_specs_desbloqueadas_20260802.md)

O Nuno (por outro canal) verificou de forma independente os 14 pontos checáveis do log acima — **14/14
batem certo** — e trouxe o texto original de quase tudo o que ficou sinalizado como "em falta". Li o
documento por inteiro (`Read`, não confiei no resumo) antes de continuar. A partir daqui, cada item usa
esse texto exato, citado onde relevante.

**[Item 12] Prompt 81 Bloco 0 (diagnóstico, 4 perguntas) — respondido por código/SQL, nenhuma resposta
adivinhada.**
1. **Gestos atuais do deck**: antes desta noite, só esquerda/direita (pass/like) existiam;
   `MatchDealDeck.tsx` já capturava `drag.y` mas só para um tilt visual cosmético (`offsetY = drag.y *
   0.35`), nunca como gesto — `onPointerUp` só olhava para `x`. Não havia "gestos ao contrário" para
   reverter — Bloco 1 era mesmo trabalho novo, não uma reversão.
2. **`matchdeal_boosts` vs `matchdeal_weekly_activity`**: `matchdeal_boosts` existe só como schema — 5
   colunas (`id, boosted_profile_id, investor_profile_id, week_start, created_at`), **0 linhas**, **0
   referências em `src`** (grep vazio). `matchdeal_weekly_activity` tem 2 linhas e está mesmo ligada a
   código — não diretamente por nome de tabela (por isso o grep inicial não achou nada), mas através das
   RPCs que o deck já chama (`matchdeal_record_exposure`, `matchdeal_record_swipe`), que a escrevem no
   lado do Postgres.
3. **`people.identity_verified` (41/44 a `true`)**: confirmado por código (`types.ts:306-310`,
   `store-supabase.tsx:556`, `store-demo.tsx:267`) — a coluna tem *default* `true` na BD; só é
   explicitamente posta a `false` num quick-create (`/log`, "Outra pessoa…") ou num convite de founder
   nunca confirmado. **Não é um sinal de verificação ativa** (não há nenhum fluxo "verificar esta
   pessoa" a marcar `true` deliberadamente) — é "não sinalizado como suspeito", não "confirmado real".
   Consequência direta para o Bloco 5: não deve ser usado como gate de moderação de foto — o próprio
   Bloco 5 já previa isto ("só gate por `identity_verified` se o Bloco 0 confirmar que é sinal genuíno;
   senão, só moderação simples") — **resposta: não é genuíno, logo é moderação simples, sem gate**.
4. **Stripe — charge avulsa ou só subscrição**: `checkout/route.ts:42` tem `form.set('mode',
   'subscription')` a fixo — nenhum código de `PaymentIntent`/checkout `mode: 'payment'` existe em lado
   nenhum do repo. Isto não é uma limitação do Stripe (Checkout Sessions suportam `mode: 'payment'` para
   cobrança avulsa) — é só código que nunca foi escrito. Relevante para o Bloco 4 (packs avulsos):
   cobrar packs a sério exigiria uma nova rota/mode, não reutilizar a subscrição existente.

**[Item 13] Prompt 78 Bloco 2 / Prompt 80 §6-14 — CORREÇÃO ao achado de ontem à noite. Já estava
fechado, não é um gap.** Ao ir escrever o filtro em `eligibleOrgIds()` que tinha proposto no Item 4,
encontrei a função Postgres `decide_investor_relationship` (chamada por `/api/portal/pipeline`
`route.ts:146` em cada Pass/Interested) — e ela **já revoga `access_grants` diretamente**:
```sql
if p_decision = 'passed' then
  update access_grants set revoked_at = now()
  where org_id = p_org_id and revoked_at is null
    and (grantee_email = any(p_investor_emails) or invited_email = any(p_investor_emails));
```
Isto corre atomicamente com o registo da decisão (mesma transação, migrations 0077/0078, AP-09) e cobre
a equipa inteira do investidor (`p_investor_emails`), não só quem clicou. Como `activeGrantOrgIds()` já
filtra por `revoked_at is null`, um Pass **já corta o acesso nas 7 rotas de portal hoje**, sem precisar
de `portal-access.ts` alguma vez consultar `investor_relationship_decisions` diretamente — a sincronia
acontece do lado da escrita (revogar o grant em si), não da leitura. **Não escrevi o filtro que tinha
proposto — seria redundante, e pior, uma segunda fonte de verdade a poder divergir da primeira.** Retiro
o achado do Item 4 de ontem: o "gap" era real ao nível do meu grep (`portal-access.ts` de facto nunca
menciona `investor_relationship_decisions`), mas não ao nível do comportamento (o acesso já está a ser
revogado). Confirmado também por código que este mecanismo já passou por "testes reais de segurança
(cancel=no-op, revocation actually blocks, race test)" antes desta noite — não é um caminho novo nem por
testar. **Bloco 2: fechado, correção registada, nenhum código push a caminho de produção porque nenhum
código foi necessário.**

**[Item 14] Prompt 71 item 7 / Prompt 36 (Investor Workspace Fase 0) — investigado por código, como
pedido ("confirmem vocês por código antes de darem como facto"). Confirmo: a Fase 0 não foi entregue
como pedida.** O Prompt 36 pedia conta+claim+verificação V0/V1, cruzamento startup/investidor, gate
D0/D1, e **billing real dos 3 planos investidor**. O que existe hoje:
- Claim/link a uma firma existente: existe (`/api/portal/investor-profile/link`,
  `/add-firm`, `/self-declare` — Prompt 63/64).
- Os 3 planos investidor existem como conceito e preço (`INVESTOR_PLANS` em `lib/plans.ts`, tiers
  `pro_scout`/`ace_spotter`/`legendary_sleuth`) — mas `InvestorPlansPanel.tsx` diz explicitamente no
  próprio comentário: **"mirrors the founder PlansPanel's request-only mode (no Stripe wiring for
  investors yet)"** — o botão escreve só `matchdeal_profiles.plan_tier_requested`, não cobra nada. Isto
  bate com o Item 4 (Stripe só tem `mode: subscription`, e mesmo essa via não está ligada ao lado
  investidor).
- Gate D0/D1 e anti-abuse baseline: não encontrei nenhuma referência a estes nomes em código — não
  localizo onde (ou se) isto foi implementado sob outro nome.
**Conclusão, com evidência, não por memória**: claim existe, billing investidor NÃO existe (só pedido
manual, tal como o Bloco 4 de packs), D0/D1/anti-abuse não localizados. Fica como achado fechado desta
verificação — decisão sobre o que fazer a seguir (construir billing real de investidor é um projeto por
si, com Stripe `mode: payment` ou subscription por tier) fica para o Nuno priorizar, não decidida aqui.

**[Item 15] Prompt 71 item 3 — as duas partes que faltavam, resolvidas por código.**
- **"Remoção de colunas pedida"**: procurei por qualquer comentário/pista de uma coluna que devesse ter
  sido removida da tabela do `/pipeline` e não encontrei nada — nem no código nem em qualquer nota
  anterior desta sessão. **Inconclusivo, não falso** — sem saber qual coluna, não posso confirmar nem
  negar. Continua sinalizado.
- **"Fix do nome de remetente" (nome, não endereço) — CONFIRMADO, é um gap real.**
  `google-oauth.ts:84`: o cabeçalho MIME é literalmente `From: ${opts.fromEmail}` — só o endereço, sem
  nenhuma parte de nome de exibição (`"Nome" <email>`). A app nunca define um nome — o que o destinatário
  vê é inteiramente o que estiver configurado no perfil da conta Gmail pareada do founder, fora do
  controlo da app. Se essa conta tiver um nome genérico ou vazio, o email chega com remetente genérico,
  exatamente a suspeita do prompt original. **Não corrigi** — mudar o remetente de outreach real tem
  implicações de confiança/entregabilidade e não tenho confirmação de que formato de nome o Nuno quer
  (`"{nome do founder}"` vs `"{nome do founder} via Sherlock Deal"` vs outro). **Proposta, não aplicada**:
  `sendGmailMessage` passa a aceitar `fromName` opcional e construir `From: "${fromName}" <${fromEmail}>`
  quando presente; a rota de send já tem `user.id` disponível para ir buscar o nome do founder
  (`org_members`/perfil) — implementação pequena assim que houver confirmação do formato.

**[Item 16] Prompt 81 Bloco 1 (gestos verticais do deck) — CONSTRUÍDO, testado, commitado e enviado.**
`MatchDealDeck.tsx`: swipe para baixo avança por 3 sub-cards estilo stories (indicador de progresso
segmentado no topo do card ativo) — sub-card 1 é a vista original (inalterada), sub-card 2 mostra
descrição completa + `team_summary`/`specific_criteria` (campos já vinham no payload da RPC mas nunca
eram renderizados em lado nenhum), sub-card 3 mostra `founded_year` + lista completa de setores/estágios.
Swipe para cima volta um sub-card, ou — no primeiro — abre uma folha "Boost this profile". Essa folha diz
honestamente "não está ativo ainda" em vez de ter um botão Confirm que não faria nada real: o Boost
(Bloco 3) continua só schema (`matchdeal_boosts`, 0 linhas, 0 código ligado), e não tinha sentido construir
um gatilho para uma ação que não existe. **Guarda respeitada**: `matchdeal_eligible_deck` (elegibilidade/
ordenação) não foi tocada — só o gesto e a UI de sub-cards.

**[Item 17] Prompt 83 Bloco 5 (export iCal da Agenda) — CONSTRUÍDO, testado, commitado e enviado.** A
computação dos itens da Agenda foi extraída para `src/lib/investor-agenda.ts` (mesmo padrão da
`investor-pipeline.ts` já usada pelo export CSV), reutilizada pela rota existente e por uma rota nova,
`/api/portal/agenda/ical`, que devolve um `.ics` (RFC 5545, com *line folding*) protegido pela mesma
sessão da app — não é uma subscrição `webcal://` ao vivo. O comentário que já existia em
`InvestorAgendaPanel.tsx` explicava porquê: uma subscrição ao vivo precisa de um token secreto por
investidor (geração/armazenamento/revogação), que é schema novo e fica por decidir — não escondido, o
comentário atualizado no próprio ficheiro deixa isso escrito. Adicionado um botão "Export .ics" no topo
do painel.

**tsc/vitest/build, todos limpos após os dois itens acima** (`tsc --noEmit` sem erros; vitest 24 ficheiros
/ 316 testes, todos a passar; `npm run build` completo sem erros). **Commits `0f0032e` (código) e
`80de4ed` (este log)**, ambos enviados.

**Deploy confirmado — mas por um método diferente do habitual, registado com transparência**: a API
pública de Deployments do GitHub ficou sem quota (`403 rate limit exceeded`, `core: 0/60`, reset só daqui
a ~50 min — esgotada pelo volume de checks desta noite inteira, confirmado via `/rate_limit`). Em vez de
esperar meia hora às cegas, confirmei o deploy por comportamento direto de produção, que é pelo menos tão
forte quanto o método habitual para estes dois itens específicos:
- `curl https://connect-b-delta.vercel.app/api/portal/agenda/ical` → `401 {"error":"Sign in first."}` —
  esta rota **não existia antes de esta noite**; um 404 significaria deploy antigo, um 401 (autenticação a
  ser exigida pelo handler novo) só é possível se o código novo já estiver a correr.
- O chunk JS de `/pair` em produção (`app/pair/page-63d58c384a951e67.js`) contém literalmente as strings
  novas `"Boost this profile"` e `"isn't live yet"` — texto que só existe no código desta noite.
- O hash do chunk partilhado `2117-c629c25be63448a7.js` servido em produção é **byte a byte o mesmo** que
  o `npm run build` local acabou de produzir.
Três confirmações independentes, todas a bater — deploy real, produção a servir o código de esta noite.
Assim que a quota da API do GitHub repuser, corro a verificação habitual por SHA como registo adicional,
mas não vejo motivo para duvidar do que já está confirmado acima.

**Por confirmar visualmente**: os dois itens acima (gestos do deck e botão de export) ainda não foram
clicados numa sessão real de browser — ficam "código correto, por confirmar visualmente" até haver esse
teste ao vivo.

**Continua sinalizado, sem construir, por falta de spec suficiente ou por decisão que só o Nuno pode
tomar**: Prompt 80 §1 (slider — texto e autorização de UX alternativa já há, mas ainda não construído,
fica para a próxima sessão dado o volume desta), Prompt 80 §4 (imagens em falta), Prompt 78 Bloco 3/4
(schemas propostos, não aplicados), Prompt 81 Bloco 2 (Hype List — falta o documento da fórmula), Bloco 3
(Boost — schema a propor, Tier A=0 por confirmar), Bloco 4 (packs — sem cobrança real sem OK), Bloco 5
(nav de rodapé — Matches/Messages são ecrãs que não existem, construir a nav sem os ecrãs seria só uma
barra morta), Prompt 83 Bloco 1/2/3 (scorecard, PDF, co-investidores), Bloco 4 (intro paths display-only),
Bloco 6 (email-in, explicitamente fora de alcance).

